let people = ['Liz', 'Dane', 'Kris'];
let lizPaysMortgage = true;

// export the variable
// exporting is similar to a function's return
// it's what's returned from the module
module.exports = {
  people: people,
  lizPaysMortgage: lizPaysMortgage
};